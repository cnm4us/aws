# Observability Policy

- Keep operation names and payload contracts stable within a phase.
- Document any naming migration in the active `plan_NN.md`.
- Validate both terminal (`debug/terminal`) and browser debug (`debug/console`) signals for user-visible regressions.
