# Handoff Process (Deprecated)

`agents/handoff/*` is legacy and no longer used for new work.

## Current Standard
- Use active `agents/implementation/plan_NN.md` as the continuity artifact.
- Record decisions, phase status, validation evidence, change log, and resume point directly in that plan.

## Historical Use
Existing `agents/handoff/Handoff_NN.md` files are retained as historical records and may be referenced for legacy context only.
