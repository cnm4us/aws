Step 02 (API)

- Lower third preset create/update now accepts `timingRule` (`first_only|entire`) and `timingSeconds` (5/10/15/20; null for `entire`).
- Responses include `timingRule` + `timingSeconds`.

